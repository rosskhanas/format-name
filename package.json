{
  "name": "format-name",
  "version": "0.0.3",
  "description": "Make correct first/last name combination among different languages including preference.",
  "keywords": [
    "format",
    "first",
    "last",
    "name"
  ],
  "author": {
    "name": "Ross Khanas",
    "email": "rtkhanas@gmail.com"
  },
  "scripts": {
    "demo": "cd demo && yarn start",
    "demo:build": "cd demo && yarn run build",
    "demo:deploy": "gh-pages -d demo/build",
    "clean": "rimraf lib",
    "lint": "eslint src",
    "test": "mocha --recursive --compilers js:babel-core/register",
    "build": "npm run lint && npm test && npm run clean && babel src --out-dir lib"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git@github.com:rtkhanas/format-name.git"
  },
  "files": [
    "lib"
  ],
  "main": "lib/index.js",
  "dependencies": {
    "lodash": "^4.17.10"
  },
  "devDependencies": {
    "babel": "^6.3.26",
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.0",
    "babel-eslint": "^8.0.0",
    "babel-preset-es2015": "^6.9.0",
    "eslint": "^4.9.0",
    "eslint-config-airbnb": "^17.0.0",
    "eslint-plugin-import": "^2.8.0",
    "gh-pages": "^2.0.0",
    "mocha": "^5.0.0",
    "rimraf": "^2.6.2"
  }
}
